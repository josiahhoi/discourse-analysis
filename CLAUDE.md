# Claude Code Rules

## Git
- Never run `git push` unless explicitly told to by the user.
- Never run `git push --force` under any circumstances.
- Commits are fine to make when asked, but always confirm before pushing.
